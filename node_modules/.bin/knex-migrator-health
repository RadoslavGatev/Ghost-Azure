#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../knex-migrator/bin/knex-migrator-health" "$@"
  ret=$?
else 
  node  "$basedir/../knex-migrator/bin/knex-migrator-health" "$@"
  ret=$?
fi
exit $ret
