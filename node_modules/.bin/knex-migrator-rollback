#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../knex-migrator/bin/knex-migrator-rollback" "$@"
  ret=$?
else 
  node  "$basedir/../knex-migrator/bin/knex-migrator-rollback" "$@"
  ret=$?
fi
exit $ret
